#include "main.h"
/**
* _islower - looks for lowercase character
* @c: the character to evaluated
* Return: 1 if c is lowercase, 0 otherwise
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
